/**
 * @type {import('next').NextConfig}
 */
const path = require('path');

const nextConfig = {
  /* config options here */
  compiler: {
    styledComponents: true
  }
}

module.exports = nextConfig;